# Application Configuration
JWT_SECRET=your-secret-key-change-in-production
ENCRYPTION_KEY=your-encryption-key-change-in-production

# AI Provider Configuration
# Options: local (Ollama), oci (Oracle Cloud Infrastructure)
AI_PROVIDER=local

# ===== Local Ollama Configuration (when AI_PROVIDER=local) =====
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# ===== OCI Configuration (when AI_PROVIDER=oci) =====
# OCI Authentication
OCI_CONFIG_FILE=~/.oci/config
OCI_PROFILE=DEFAULT

# OCI Generative AI Service
OCI_GENAI_ENDPOINT=https://inference.generativeai.us-chicago-1.oci.oraclecloud.com
OCI_COMPARTMENT_ID=ocid1.compartment.oc1..your-compartment-id
OCI_MODEL_ID=cohere.command-r-plus

# Available OCI Models:
# - cohere.command-r-plus (Recommended for general use)
# - meta.llama-3-70b-instruct
# - cohere.command-r-16k

# OCI Embedding Service
OCI_EMBEDDING_ENDPOINT=https://inference.generativeai.us-chicago-1.oci.oraclecloud.com
OCI_EMBEDDING_MODEL_ID=cohere.embed-english-v3.0

# ===== Vector Store Configuration =====
VECTOR_STORE_TYPE=in-memory
# CHROMA_URL=http://localhost:8001

# ===== Database Configuration =====
# SQLite is used by default, no configuration needed
# Database file will be created at: ./data/intelligence.db

# ===== CORS Configuration =====
CORS_ALLOWED_ORIGINS=http://localhost:3000
